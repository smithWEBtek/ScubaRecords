<div class="container mt-5">
  <div class="row">
    <div class="col-12 col-lg-6 d-flex justify-content-between align-items-center">
      <h3>Dive Sites</h3>
    </div>
    <div class="col-12 col-lg-6 align-items-center">
      <%= form_tag(sites_path, method: :get) do %>
        <div class="form d-flex align-items-center">
          <%= text_field_tag :site_name, params[:site_name], placeholder: "Search by name or location", class: "form-control form-control-sm" %>
          <%= submit_tag 'Search', name: nil, class: "btn btn-info btn-sm" %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="mt-3">
    <% @sites.each do |site| %>
      <p>Name: <%= link_to site.name, site_path(site) %></p>
      <p>Location: <%= site.location %></p>
      <hr>
    <% end %>
  </div>

</div>
